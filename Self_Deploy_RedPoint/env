# Configure these variables with your information
# Follow the Self-Deploy-README for instructions

# Your own domain including www:
ROOT=www.yourdomain.com

# Your own domain excluding www:
ROOT_WITHOUT_SUBDOMAIN=yourdomain.com

# Username and password for your Mongo Database:
MONGO_USERNAME=yourUsername
MONGO_PASSWORD=123fakestreet

# Password for Redis:
REDIS_PW=password_from_redis_config

# Path to SSL Certification and Key generated by Certbot (for HTTPS):
SSLKEY=path/to/privatekey
SSLCERT=path/to/fullchain.pem

# Email address used in 'from' field:
EMAIL_USER=you@yourdomain.com

# API key provided by SendGrid:
SENDGRID_API_KEY=key

# Name of the sessions object you created in Redis:
SESSIONS_OBJ=redis_sessions_hash_name

# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

# These defaults don't need your configuration

# Default node port
PORT=8000

# Default RedPoint docker image
IMAGE=redpointnotebooks/docker

# Default internal network IP address
MONGO_HOSTNAME=127.0.0.1
REDIS_HOST=127.0.0.1

# Default Redis Port
REDIS_PORT=6379

# Name of webhook queue in Redis
QUEUENAME=webhooks

# Namespace for Redis simple messaging queue
NAMESPACE=rsmq

NODE_ENV=production

# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
